<h2>Users</h2>

<%= link_to 'new user', '/users/new' %>

<% @users.each do |user| %>

  <div class="row">
    <a href="/users/<%= user.id %>">
      <%= user.email %>
    </a>
     
    <%= link_to 'edit', edit_user_path(user) %>
  </div>

<% end %>